# 대화 모델 기기 연결 (2026-09-23)

모델: Qwen3-0.6B + golf_assistant_v1 adapter, Q8_0 GGUF.
크기 639446752 bytes.
SHA256 0d64feb2fe193da37efd64aeade25726e5b289e907ab1922920258325f199452.
llama.cpp 변환 revision: 79bfc1d43a2e1e790f455522e0b4edbef7e9d22c.

기존 측정 설명 모델과 별도 파일/채널을 사용합니다.
설정의 모델 설정 화면에서 대화 모델 파일을 등록할 수 있습니다.
기존 원본 모델도 유지됩니다. 가져온 모델은 크기와 SHA256을 검사합니다.

규칙이 처리한 질문/명령은 기존처럼 응답/실행합니다. unknown 요청만
AssistantModelService로 전달하고 최신 관련 기능 규칙을 프롬프트에 넣습니다.
모델이 선택한 등록 기능을 버튼으로 제안합니다. 모델의 자유 생성 문장을
사용자에게 그대로 표시하거나 모델 명령만으로 설정을 변경하지 않습니다.
이는 새 모델의 미해결 명령 오류를 고려한 후보 기능 제안 단계입니다.
clarify/부정/복합 요청은 SLM으로 자동 실행하지 않습니다.

검증: SM-F741N 앱 내부 MethodChannel과 APK의 llama 실행 엔진을 통해
실제 GGUF 추론 성공. '갤러리에서 저장된 영상 선택해줘' 요청에 갤러리 버튼
제안, 최초 호출 6772ms. 파일 해시 일치, APK 설치 성공(148489483 bytes).
고정 테스트는 debug-only service extension을 사용했습니다. 실제 음성 입력
전체 경로 및 다양한 자연어에 대한 정확도/지연 검증을 뜻하지 않습니다.

대화 기록/측정 수치 전체를 새 모델로 설명하는 기능은 아직 연결하지 않았습니다.
현 단계는 규칙 기반 설명과 후보 기능 추천입니다.
